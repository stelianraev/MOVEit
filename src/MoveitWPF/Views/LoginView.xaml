<UserControl x:Class="MoveitWpf.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MoveitWpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Visibility="{Binding LoginFieldsVisibility}">
            <TextBox x:Name="Username" Grid.Row="0" Width="250" Margin="10"
              Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

            <PasswordBox x:Name="Password" Grid.Row="1" Width="250" Margin="10"
                     PasswordChanged="PasswordBox_PasswordChanged"/>

            <Button x:Name="LoginBtn" Grid.Row="2" Content="Login" Width="100" Margin="10"
             Command="{Binding LoginCommand}"/>
        </StackPanel>

        <TreeView Visibility="{Binding TreeViewVisibility}" Grid.RowSpan="3">
            <TreeView x:Name="FileTree" Visibility="Hidden"/>
        </TreeView>
    </Grid>
</UserControl>
